(function() {
  'use strict';

  angular.module('util.growl')
  .controller('GrowlService', GrowlService);

  // GrowlController.$inject = [];
  function GrowlService(){
    // growlProvider.globalTimeToLive(5000);
    // growlProvider.messagesKey("responses");
    // growlProvider.messageTextKey("msg");
    // growlProvider.messageSeverityKey("type");
    // growlProvider.onlyUniqueMessages(true);

    // var vm = this;
    // vm.showWarning = showWarning;
    // vm.showInfo = showInfo;
    // vm.showSuccess = showSuccess;
    // vm.showError = showError;

    // function showWarning(title, message){
    //   $scope.addSpecialWarnMessage = function() {
    //     growl.warning(message, {title: title});
    //   }
    // }
  }
})();
